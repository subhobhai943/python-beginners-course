# Lesson 8: Strings in Depth ✂️

Welcome to Lesson 8! Let's master strings — text data. Manipulate, slice, and format them!

## 🔤 String Basics
Strings are just text in quotes:

```python
name = "Alex"
```

## ✂️ Slicing and Indexing
Get parts of strings:

```python
word = "Python"
print(word[0])      # 'P'
print(word[2:5])    # 'tho'
```

## 🔄 String Methods
Useful tools:
- `.upper()`, `.lower()`, `.title()`, `.replace()`, `.find()`, `.split()`

```python
msg = "hello world"
print(msg.upper())
```

## 🎨 Formatting
Combine with variables:
```python
name = "Alex"
age = 15
print(f"My name is {name} and I'm {age}")
```

## 🎯 Key Takeaways
✅ Strings hold text
✅ Slice, change, and format strings
✅ Tons of built-in tools!
---